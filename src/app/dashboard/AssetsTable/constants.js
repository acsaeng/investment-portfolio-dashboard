const TABLE_TITLE = 'Your portfolio';
const BUTTON_LABEL = {
  ADD_ASSET: 'Add asset',
  DELETE_ASSET: 'Delete',
};

const TABLE_HEADER = {
  symbol: '',
  assetPrice: 'Current price',
  totalValue: 'Total value',
  return: 'All time return',
  actions: '',
};

const NO_DATA_RESPONSE = 'No data to show';

export { BUTTON_LABEL, NO_DATA_RESPONSE, TABLE_HEADER, TABLE_TITLE };
